[project]
name = "rokuos-perfetto-utils"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "perfetto>=0.16.0",
    "websocket>=0.2.1",
]

[project.scripts]
find-brs-cycles = "find_brs_cycles:main"
